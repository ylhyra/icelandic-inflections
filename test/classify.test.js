import assert from 'assert'
import classify from './classification/BIN_classification'

// it('splitOnVowels', () => {
//   assert.deepEqual(splitOnVowels('Kjartan'), ['K', 'ja', 'rt', 'a', 'n'])
//   assert.deepEqual(splitOnVowels('andrés'), ['', 'a', 'ndr', 'é', 's'])
//   return
// })
